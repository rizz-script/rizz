# RizzScript (v0.1)

This repo contains a minimal interpreter for the **RizzScript** language described in `SPEC.md`.

## Run

```bash
cargo run -- run examples/hello.rizz
```

Install a local `rizz` binary:

```bash
cargo install --path .
rizz run examples/hello.rizz
```

